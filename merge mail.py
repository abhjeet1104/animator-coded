with open ("name.txt", 'r' , encoding = 'utf -8') as names file
           #open body.txt for reading
           with open ("body.txt" 'r' encoding = 'utf -8')as body_file :
           #read enire content of the body
           body = body.file. read()
           #iterate over names
           for name in name.file :

           mail= "hello"+name + body
           #write the mail to individual files
           with open (name.srip()+, ".txt" , 'w' encoding ='utf-8') as mail file

                   mail_file.write (mail).
